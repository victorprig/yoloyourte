
<div class="container text-center">
  <div class="row">
    <div class="col-xs-4 col-xs-offset-4" style="margin-top: 160px">
      <div>
        First Name:  <%= current_user.profile.last_name %>
      </div>
      <hr>

      <div>
        Last Name:  <%= current_user.profile.last_name %>
      </div>
      <hr>

      <div>
        Address:  <%= current_user.profile.address %>
      </div>
      <hr>

      <div>
        Born:  <%= current_user.profile.last_name %>
      </div>
      <hr>

      <%= link_to "Edit Profile", edit_profile_path(current_user.profile), :method => :get, class: 'btn btn-info full-width' %>
      <%= link_to("See YOUR Bookings", bookings_path, class: 'btn btn-info full-width') %>
    </div>
  </div>
</div>
